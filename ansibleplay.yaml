---
  - name: Deploy App
    hosts: node1
    gather_facts: false
    tasks:
      - name: Copying Docker Compose File to Host
        copy:
          src: docker-compose.yaml
          dest: /home/ubuntu/docker-compose.yaml
      - name: Start Services
        shell: docker-compose up -d
